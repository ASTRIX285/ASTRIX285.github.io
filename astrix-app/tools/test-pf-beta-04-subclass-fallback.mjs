import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';

const ROOT=new URL('../',import.meta.url);
const FIXTURES_URL=new URL('data/paradox-forge/beta/ASTRIX_Paradox_Forge_Beta_Fixtures_v1.json',ROOT);
const IDENTITIES_URL=new URL('data/paradox-forge/beta/beta-component-identities.json',ROOT);
const MANIFEST_URL=new URL('data/paradox-forge/beta/beta-bungie-manifest-cache.json',ROOT);
const TRAIT_DIRECTION_URL=new URL('data/paradox-forge/beta/beta-bungie-manifest-cache-trait-direction-extension.json',ROOT);
const LOADER_URL=new URL('pages/guardian-workspace-v2/guardian-fixture-loader.mjs',ROOT);
const ENGINE_URL=new URL('pages/guardian-workspace-v2/guardian-paradox-engine.mjs',ROOT);
const readJson=async url=>JSON.parse(await readFile(url,'utf8'));
const fixtureLibrary=await readJson(FIXTURES_URL);
const listeners=new Map();
globalThis.document={readyState:'loading',addEventListener(type,handler){const rows=listeners.get(type)??[];rows.push(handler);listeners.set(type,rows);},dispatchEvent(event){for(const handler of listeners.get(event.type)??[])handler(event);return true;},getElementById(){return null;},querySelector(){return null;}};
globalThis.CustomEvent=class CustomEvent{constructor(type,init={}){this.type=type;this.detail=init.detail;}};
globalThis.fetch=async url=>{const value=String(url);let target=null;if(value.includes('ASTRIX_Paradox_Forge_Beta_Fixtures_v1.json'))target=FIXTURES_URL;else if(value.includes('beta-component-identities.json'))target=IDENTITIES_URL;else if(value.includes('beta-bungie-manifest-cache-trait-direction-extension.json'))target=TRAIT_DIRECTION_URL;else if(value.includes('beta-bungie-manifest-cache.json'))target=MANIFEST_URL;if(!target)return {ok:false,status:404,json:async()=>({})};const data=await readJson(target);return {ok:true,status:200,json:async()=>data};};
const [{loadBetaFixture},{analyzeGuardianBuild}]=await Promise.all([import(LOADER_URL.href),import(ENGINE_URL.href)]);
const detail=await loadBetaFixture('PF-BETA-04');
const analysis=analyzeGuardianBuild(detail);
console.log(JSON.stringify({aspects:detail.aspects.map(x=>({hash:x.hash,name:x.name,description:x.description,officialDescription:x.officialDescription,traitIds:x.traitIds,officialTraitIds:x.official?.traitIds})),buildLoop:analysis.buildLoop,candidates:analysis.candidateRelationships,confidence:analysis.confidence},null,2));
assert.ok(analysis.buildLoop.some(link=>Number(link.from?.hash)===4194622037&&link.output==='jolt'&&Number(link.to?.hash)===4194622036),'Expected Tempest Strike -> jolt -> Flow State link is missing');
