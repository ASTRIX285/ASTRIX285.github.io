# Guardian Workspace Prototype Archive

This folder contains the original Guardian Workspace prototype used to validate the early data contract and three-column composition.

Status: archived reference only.

Do not extend this implementation for production. The production build now lives at:

`astrix-app/pages/guardian-workspace-v1/`

The prototype remains intact so its early layout and preview-state experiments can be compared during migration.
