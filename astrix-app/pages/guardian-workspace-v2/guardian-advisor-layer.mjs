import './guardian-gear-layout.mjs';
const SUBCLASS_NAMES={hunter:{void:'Nightstalker',solar:'Gunslinger',arc:'Arcstrider',stasis:'Revenant',strand:'Threadrunner',prismatic:'Prismatic Hunter'},titan:{void:'Sentinel',solar:'Sunbreaker',arc:'Striker',stasis:'Behemoth',strand:'Berserker',prismatic:'Prismatic Titan'},warlock:{void:'Voidwalker',solar:'Dawnblade',arc:'Stormcaller',stasis:'Shadebinder',strand:'Broodweaver',prismatic:'Prismatic Warlock'}};
const state={characterClass:'hunter',subclass:'void',artifact:null,recommendedArtifactPerks:null,super:null};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
const item=v=>!v?null:typeof v==='string'?{name:v,icon:''}:{...v,name:v.name??v.displayName??v.label??'',icon:v.icon??v.iconUrl??v.displayProperties?.icon??''};
const list=v=>Array.isArray(v)?v.map(item).filter(Boolean):[];
function tile(v,extra=''){const x=item(v);if(!x)return'';return `<span class="ico-badge ${extra}" tabindex="0" title="${esc(x.name||'Loaded item')}" aria-label="${esc(x.name||'Loaded item')}">${x.icon?`<img src="${esc(x.icon)}" alt="" onerror="this.style.display='none';this.nextElementSibling.style.display='grid'">`:''}<span class="ph-glyph" style="${x.icon?'display:none':''}">◆</span></span>`}
function abilityData(data){const a=data.abilities&&typeof data.abilities==='object'&&!Array.isArray(data.abilities)?data.abilities:{};const explicitSuper=item(data.super??a.super);let all=Array.isArray(data.abilities)?list(data.abilities):[data.classAbility??a.classAbility??a.class,data.movement??a.movement,data.melee??a.melee,data.grenade??a.grenade].map(item).filter(Boolean);if(!explicitSuper&&all.length>4)return {superItem:all[0],abilities:all.slice(1,5)};return {superItem:explicitSuper,abilities:all.filter(x=>x?.name!==explicitSuper?.name).slice(0,4)}}
function moveSubclassIntoHero(){const stage=document.querySelector('.stage');const hero=document.querySelector('.subclass-hero');if(!stage||!hero)return null;let dock=document.getElementById('heroSubclassDock');if(!dock){dock=document.createElement('div');dock.id='heroSubclassDock';dock.className='hero-subclass-dock';stage.appendChild(dock)}if(hero.parentElement!==dock)dock.appendChild(hero);return dock}
function ensureSuperFocus(){const left=document.querySelector('.left');if(!left)return null;let focus=document.getElementById('superFocus');if(!focus){focus=document.createElement('button');focus.id='superFocus';focus.className='super-focus';focus.type='button';focus.innerHTML='<span class="super-focus-icon"></span><span class="super-focus-copy"><small>SUPER</small><b>Loaded Super</b><em>Change</em></span>';focus.addEventListener('click',()=>document.dispatchEvent(new CustomEvent('astrix:request-super-change',{detail:{characterClass:state.characterClass,subclass:state.subclass,super:state.super}})))}if(focus.parentElement!==left){const firstGroup=left.querySelector('.grp');firstGroup?left.insertBefore(focus,firstGroup):left.prepend(focus)}return focus}
function renderSuper(superItem){const focus=ensureSuperFocus();if(!focus)return;state.super=superItem||null;const icon=focus.querySelector('.super-focus-icon');const name=focus.querySelector('b');if(name)name.textContent=superItem?.name||'Super not loaded';if(icon)icon.innerHTML=superItem?tile(superItem,'super-main'):tile({name:'Super not loaded',icon:''},'super-main')}
function renderFragments(data){const fragments=list(data.fragments).slice(0,5);const supplied=Number(data.fragmentCapacity??data.fragmentSlots??data.maxFragments);const capacity=Number.isFinite(supplied)?Math.max(0,Math.min(5,supplied)):Math.max(4,Math.min(5,fragments.length));const html=Array.from({length:5},(_,i)=>{const f=fragments[i];if(f&&i<capacity)return `<div class="slot">${tile(f)}</div>`;const label=i<capacity?'Empty fragment slot':'Fragment slot unavailable with equipped aspects';return `<div class="slot fragment-locked" title="${esc(label)}" aria-label="${esc(label)}"><span class="ico-badge"><span class="ph-glyph">◆</span></span></div>`}).join('');const el=document.getElementById('fragList');if(el)el.innerHTML=html}
function moveStatsIntoHero(){const stage=document.querySelector('.stage');const statsRow=document.getElementById('statsRow');const statsCard=statsRow?.closest('.eq');if(!stage||!statsCard||statsCard.classList.contains('hero-stats'))return;statsCard.classList.add('hero-stats');stage.appendChild(statsCard)}
function render(data={}){Object.assign(state,data);const cls=String(data.characterClass??data.className??state.characterClass).toLowerCase();const sub=String(data.subclass??state.subclass).toLowerCase();state.characterClass=cls;state.subclass=sub;const subclassName=data.subclassName??data.subclassDisplayName??SUBCLASS_NAMES[cls]?.[sub]??`${sub} ${cls}`;const hero=document.querySelector('.subclass-hero');if(hero){const b=hero.querySelector('b');if(b)b.textContent=subclassName.toUpperCase();hero.dataset.subtitle=`${cls.toUpperCase()} SUBCLASS`}const crest=document.getElementById('scCrest');const crestUrl=data.subclassIcon??data.crest??data.subclassCrest;if(crest&&crestUrl){crest.style.display='';crest.src=crestUrl;crest.alt=`${subclassName} subclass icon`}moveSubclassIntoHero();const resolved=abilityData(data);if(resolved.superItem)renderSuper(resolved.superItem);else ensureSuperFocus();if(resolved.abilities.length){const abilityList=document.getElementById('abilityList');if(abilityList)abilityList.innerHTML=Array.from({length:4},(_,i)=>`<div class="ability-row">${resolved.abilities[i]?tile(resolved.abilities[i]):tile({name:'Ability slot awaiting loaded Guardian data',icon:''},'empty')}</div>`).join('')}if(Array.isArray(data.aspects)){const aspects=list(data.aspects).slice(0,4);document.getElementById('aspectList').innerHTML=aspects.map(x=>`<div class="slot">${tile(x)}</div>`).join('')}if(Array.isArray(data.fragments))renderFragments(data);const artifact=item(data.artifact??state.artifact);if(artifact){state.artifact=artifact;const n=document.getElementById('artName'),im=document.getElementById('artIcon');if(n)n.textContent=artifact.name||'Loaded Artifact';if(im){im.src=artifact.icon||'';im.alt=artifact.name||'Loaded Artifact'}}const recSource=data.recommendedArtifactPerks??data.artifactRecommendations??data.recommendedArtifactMods??state.recommendedArtifactPerks;if(Array.isArray(recSource)){state.recommendedArtifactPerks=recSource;const p=document.getElementById('artPerks');if(p)p.innerHTML=list(recSource).map(x=>x.icon?`<img src="${esc(x.icon)}" alt="" tabindex="0" title="${esc(x.name||'Recommended artifact perk')}" aria-label="${esc(x.name||'Recommended artifact perk')}" onerror="this.style.display='none'">`:tile(x)).join('')}const sw=document.querySelector('.char-switch b');if(sw)sw.textContent=`${cls.charAt(0).toUpperCase()+cls.slice(1)} ▾`;moveStatsIntoHero()}
function bootstrapPreview(){moveSubclassIntoHero();const abilityList=document.getElementById('abilityList');const rows=abilityList?[...abilityList.querySelectorAll('.ability-row')]:[];if(rows.length>=5){const superRow=rows[0];const img=superRow.querySelector('img');const name=superRow.querySelector('.meta b')?.textContent?.trim()||img?.alt||'Loaded Super';const superItem={name,icon:img?.src||''};renderSuper(superItem);rows.slice(1,5).forEach(r=>r.querySelector('.meta')?.remove());abilityList.innerHTML=rows.slice(1,5).map(r=>r.outerHTML).join('')}else ensureSuperFocus();const existingFragments=[...document.querySelectorAll('#fragList .slot')];if(existingFragments.length&&existingFragments.length<5){const capacity=Math.max(4,existingFragments.length);for(let i=existingFragments.length;i<5;i++)document.getElementById('fragList')?.insertAdjacentHTML('beforeend',`<div class="slot fragment-locked" title="${i<capacity?'Empty fragment slot':'Fragment slot unavailable with equipped aspects'}"><span class="ico-badge"><span class="ph-glyph">◆</span></span></div>`)}moveStatsIntoHero()}
document.addEventListener('astrix:guardian-selection-changed',e=>render(e.detail||{}));
document.addEventListener('astrix:artifact-recommendations-changed',e=>render(e.detail||{}));
bootstrapPreview();
