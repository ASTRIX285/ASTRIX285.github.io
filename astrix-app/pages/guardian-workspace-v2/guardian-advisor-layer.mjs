/* Advisor prose layer intentionally removed.
   The Guardian Workspace left rail now mirrors the loaded Guardian directly.
   Artifact recommendations are rendered as verified perk icons only by guardian-workspace-v2.mjs. */
