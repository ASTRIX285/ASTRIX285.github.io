name: ASTRIX Probe Current Artifact V2
on:
  push:
    branches: [feature/astrix-multigame-platform]
    paths: ['.github/workflows/astrix-probe-current-artifact-v2.yml']
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/astrix-multigame-platform
      - name: Probe live artifact tiers
        run: python3 .astrix-community/probe-current-artifact-v2.py
